{% extends 'layout1.html.twig' %}

{% block title %}Categoria{% endblock %}

{% block contenido %}
    <h1>Categoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ categorium.categoria }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin') }}" class="pt-2 tm-mb-40 tm-color-primary tm-post-title">back to list</a>

    <a href="{{ path('categoria_edit', {'id': categorium.id}) }}" class="pt-2 tm-mb-40 tm-color-primary tm-post-title">edit</a>

    {{ include('categoria/_delete_form.html.twig') }}
{% endblock %}
